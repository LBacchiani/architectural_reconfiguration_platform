apiVersion: v1
kind: Pod
metadata:
  name: multiple-orchestrator
  labels:
    app: orchestrator
spec:
  affinity:
    nodeAffinity:
      preferredDuringSchedulingIgnoredDuringExecution:
        - weight: 1
          preference:
            matchExpressions:
              - key: zone
                operator: In
                values:
                  - cloud
  containers:
    - image: giovaz94/birex-orchestrator
      name: birex-orchestrator
      imagePullPolicy: Always
      ports:
        - containerPort: 8081
